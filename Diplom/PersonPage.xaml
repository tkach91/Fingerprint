<Window x:Class="Diplom.PersonPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PersonPage" Height="452" Width="349">
    <Grid>
        <DataGrid AutoGenerateColumns="False" Height="391" HorizontalAlignment="Left" IsReadOnly="True" ItemsSource="{Binding}" Margin="10,10,0,0" Name="dataGridAllLocations" SelectionMode="Single" VerticalAlignment="Top" Width="305">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Помещения к которым предоставлен доступ" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
